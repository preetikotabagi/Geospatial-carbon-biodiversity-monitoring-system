# Darukaa.Earth — Frontend

React + Vite frontend for the Darukaa.Earth monitoring dashboard.

See the [root README](../README.md) for full project documentation,
setup instructions, and architecture notes.

Quick start:

```bash
npm install
cp .env.example .env   # then fill in VITE_MAPBOX_TOKEN
npm run dev
```
